{
  "private": true,
  "dependencies": {
    "@sap/approuter": "^10.2.0",
    "dotenv": "^8.2.0",
    "npm-run-all": "^4.1.5"
  },
  "scripts": {
    "run:proxy": "node ../bin/cfdp.js run",
    "run:approuter": "node --require dotenv/config node_modules/@sap/approuter/approuter.js",
    "start": "run-p run:*"
  }
}

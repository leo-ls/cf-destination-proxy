{
  "authenticationMethod": "route",
  "sessionTimeout": 15,
  "routes": [
    {
      "source": "^/([^/]+)(.*)$",
      "destination": "$1",
      "target": "$2",
      "authenticationType": "none"
    }
  ]
}
